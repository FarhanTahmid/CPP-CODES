Write C++ program to read an array of 10 elements (positive and negative integers), if the number is odd substitute with its square then sort and print the array?

Note:- don't use functions or files, all the code in the main program.